<Window x:Class="PersonaEditor.Controls.ToolBox.Resize"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonaEditor.Controls.ToolBox"
        mc:Ignorable="d"
        Title="Resize" Height="100" Width="150" WindowStyle="None" ResizeMode="NoResize" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">
    <Border BorderBrush="Black" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Width="50" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="5" x:Name="TextB" x:FieldModifier="private" TextChanged="TextB_TextChanged"/>
            <Button Grid.Row="1" Width="50" Margin="5" Content="OK" Click="Button_Click"/>
            <Button Grid.Row="1" Grid.Column="1"  Width="50" Margin="5" Content="Cancel" Click="Button_Click_1"/>
        </Grid>
    </Border>
</Window>
